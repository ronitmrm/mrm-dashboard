# Offer letter artwork

Header and footer cropped from the user-supplied `Offer.pdf` on 2026-09-19,
rendered at 200 dpi. These contain company branding only; the sample candidate,
reference, signature and stamp are excluded. Reused on newly generated offers.
Typography uses the existing Outfit fonts in `lib/pricing/assets`.
