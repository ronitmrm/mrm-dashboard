# Production Start Forecast Locks on Actual Start

Planned production start is a forecast until production actually starts. If setup finishes later than planned, the forecast moves to the setup completion date and planned production end recalculates; once actual production is recorded, the production start is locked to that actual start date and only the planned production end remains dynamic.
