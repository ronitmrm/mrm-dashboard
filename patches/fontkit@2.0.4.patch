diff --git a/dist/browser-module.mjs b/dist/browser-module.mjs
index 4d0a389de8a3db767f1fef99ed186790431aa844..a97b50edd82fe8eb2934ff325358d565b06ba4f5 100644
--- a/dist/browser-module.mjs
+++ b/dist/browser-module.mjs
@@ -12098,6 +12098,9 @@ class $807e58506be70005$export$2e2bcd8739ae039 {
             }
             if (c.command === 'closePath') endPtsOfContours.push(pointCount - 1);
         }
+        // Flush the last repeated flag before coordinate bytes.
+        if (same > 0) flags.push(same);
+
         // Close the path if the last command didn't already
         if (path.commands.length > 1 && path.commands[path.commands.length - 1].command !== 'closePath') endPtsOfContours.push(pointCount - 1);
         let bbox = path.bbox;
@@ -12115,7 +12118,7 @@ class $807e58506be70005$export$2e2bcd8739ae039 {
         };
         let size = $807e58506be70005$var$Glyf.size(glyf);
         let tail = 4 - size % 4;
-        let stream = new $6uUbQ$EncodeStream(size + tail);
+        let stream = new $6uUbQ$EncodeStream(new Uint8Array(size + tail));
         $807e58506be70005$var$Glyf.encode(stream, glyf);
         // Align to 4-byte length
         if (tail !== 0) stream.fill(0, tail);
diff --git a/dist/browser.cjs b/dist/browser.cjs
index a5fa901b5608575f84390e3ef0474a7789e8269a..5d7136c897a70263d35e14bdb930f3d118b1eec2 100644
--- a/dist/browser.cjs
+++ b/dist/browser.cjs
@@ -12115,6 +12115,9 @@ class $2784eedf0b35a048$export$2e2bcd8739ae039 {
             }
             if (c.command === 'closePath') endPtsOfContours.push(pointCount - 1);
         }
+        // Flush the last repeated flag before coordinate bytes.
+        if (same > 0) flags.push(same);
+
         // Close the path if the last command didn't already
         if (path.commands.length > 1 && path.commands[path.commands.length - 1].command !== 'closePath') endPtsOfContours.push(pointCount - 1);
         let bbox = path.bbox;
@@ -12132,7 +12135,7 @@ class $2784eedf0b35a048$export$2e2bcd8739ae039 {
         };
         let size = $2784eedf0b35a048$var$Glyf.size(glyf);
         let tail = 4 - size % 4;
-        let stream = new $gfJaN$restructure.EncodeStream(size + tail);
+        let stream = new $gfJaN$restructure.EncodeStream(new Uint8Array(size + tail));
         $2784eedf0b35a048$var$Glyf.encode(stream, glyf);
         // Align to 4-byte length
         if (tail !== 0) stream.fill(0, tail);
diff --git a/dist/main.cjs b/dist/main.cjs
index 851b2d2bbcae17e91f9f2468f61112ee2dc8fbd8..8f6916d4fc69cd7443baa90ae43e9aee51439973 100644
--- a/dist/main.cjs
+++ b/dist/main.cjs
@@ -12136,6 +12136,9 @@ class $2784eedf0b35a048$export$2e2bcd8739ae039 {
             }
             if (c.command === 'closePath') endPtsOfContours.push(pointCount - 1);
         }
+        // Flush the last repeated flag before coordinate bytes.
+        if (same > 0) flags.push(same);
+
         // Close the path if the last command didn't already
         if (path.commands.length > 1 && path.commands[path.commands.length - 1].command !== 'closePath') endPtsOfContours.push(pointCount - 1);
         let bbox = path.bbox;
@@ -12153,7 +12156,7 @@ class $2784eedf0b35a048$export$2e2bcd8739ae039 {
         };
         let size = $2784eedf0b35a048$var$Glyf.size(glyf);
         let tail = 4 - size % 4;
-        let stream = new $elh9A$restructure.EncodeStream(size + tail);
+        let stream = new $elh9A$restructure.EncodeStream(new Uint8Array(size + tail));
         $2784eedf0b35a048$var$Glyf.encode(stream, glyf);
         // Align to 4-byte length
         if (tail !== 0) stream.fill(0, tail);
diff --git a/dist/module.mjs b/dist/module.mjs
index fde88fc3f23e6fff533da842186fee8402cbcde1..c2905f9984982ff92f39a76a8b48e17dec16d08c 100644
--- a/dist/module.mjs
+++ b/dist/module.mjs
@@ -12119,6 +12119,9 @@ class $807e58506be70005$export$2e2bcd8739ae039 {
             }
             if (c.command === 'closePath') endPtsOfContours.push(pointCount - 1);
         }
+        // Flush the last repeated flag before coordinate bytes.
+        if (same > 0) flags.push(same);
+
         // Close the path if the last command didn't already
         if (path.commands.length > 1 && path.commands[path.commands.length - 1].command !== 'closePath') endPtsOfContours.push(pointCount - 1);
         let bbox = path.bbox;
@@ -12136,7 +12139,7 @@ class $807e58506be70005$export$2e2bcd8739ae039 {
         };
         let size = $807e58506be70005$var$Glyf.size(glyf);
         let tail = 4 - size % 4;
-        let stream = new $52ZIf$EncodeStream(size + tail);
+        let stream = new $52ZIf$EncodeStream(new Uint8Array(size + tail));
         $807e58506be70005$var$Glyf.encode(stream, glyf);
         // Align to 4-byte length
         if (tail !== 0) stream.fill(0, tail);
diff --git a/src/glyph/TTFGlyphEncoder.js b/src/glyph/TTFGlyphEncoder.js
index 6ab8b73c6532146442ea1564aa56dca417b369b9..b6e72387828771afc806235b89197f664939433c 100644
--- a/src/glyph/TTFGlyphEncoder.js
+++ b/src/glyph/TTFGlyphEncoder.js
@@ -105,3 +105,6 @@ export default class TTFGlyphEncoder {
+    // Flush the last repeated flag before coordinate bytes.
+    if (same > 0) flags.push(same);
+
     // Close the path if the last command didn't already
     if (path.commands.length > 1 && path.commands[path.commands.length - 1].command !== 'closePath') {
       endPtsOfContours.push(pointCount - 1);
@@ -124,7 +127,7 @@ export default class TTFGlyphEncoder {
     let size = Glyf.size(glyf);
     let tail = 4 - (size % 4);
     
-    let stream = new r.EncodeStream(size + tail);
+    let stream = new r.EncodeStream(new Uint8Array(size + tail));
     Glyf.encode(stream, glyf);
     
     // Align to 4-byte length
