[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter @workspace/runtime... build"

[deploy]
startCommand = "pnpm runtime:worker"
numReplicas = 1
sleepApplication = false
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
overlapSeconds = 0
drainingSeconds = 10
